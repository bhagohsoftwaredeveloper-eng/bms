# Only needed when running the admin-web dev server against an API on a
# different origin. When the NestJS backend serves the built admin-web
# (the production setup), requests go to the same origin's /api and this
# can stay unset.
VITE_API_URL=http://localhost:3001/api
